import React from 'react';
import ReactDOM from 'react-dom';

import './Body.scss'

ReactDOM.render(
    <React.StrictMode>
        asd
    </React.StrictMode>,
    document.getElementsByTagName("body")
)